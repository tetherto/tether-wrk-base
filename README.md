# tether-wrk-base

A base worker class, extending `bfx-wrk-base`.

This class is intended to be used as a base worker for all hrpc workers.

It includes two facilities:
1. `hp-svc-facs-store`
2. `hp-svc-facs-net`
